'use client'
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { supabase } from '@/lib/supabase';
export default function Signup(){const [form,setForm]=useState({full_name:'',email:'',password:'',organization:''});const [msg,setMsg]=useState('');const [err,setErr]=useState('');const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setErr('');setMsg('');const {error}=await supabase.auth.signUp({email:form.email,password:form.password,options:{data:{full_name:form.full_name,organization:form.organization}}});if(error)setErr(error.message);else{setMsg('Registration successful. If email confirmation is enabled, check your email before signing in.');setTimeout(()=>router.push('/login'),1200)}}return <main className="container"><div className="login card"><h1 className="title">Create Account</h1><p className="subtitle">Register as a performer/user.</p>{err&&<div className="error">{err}</div>}{msg&&<div className="notice">{msg}</div>}<form onSubmit={submit}><div className="field"><label>Full name</label><input required value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></div><div className="field"><label>Email</label><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="field"><label>Organization / Group</label><input value={form.organization} onChange={e=>setForm({...form,organization:e.target.value})} placeholder="e.g. QSU Marching Band"/></div><div className="field"><label>Password</label><input type="password" minLength={8} required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><button className="btn">Register</button></form><p className="small muted"><a href="/login">Already have an account? Sign in.</a></p></div></main>}
